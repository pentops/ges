package gestest.v1

entity Foo {
	| Foo is lorem ipsum

	key fooId key:id62

	data name string

	status ACTIVE
	status INACTIVE

	event Create {
		field name string
	}

	event Archive {
	}

	summary Summary {
		field name string
	}
}

topic Generic publish {
	message Foo {
		field fooId ! key:id62
		field name ! string
	}
}
